import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  searchView: {
    marginTop: 20,
  },
  mainContent: {
    // alignItems: 'center',
    marginTop: 15,
  },
});

export default styles;
