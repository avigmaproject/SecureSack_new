import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  key: {
    flexWrap: 'wrap',
    paddingHorizontal: 5,
  },
  container: {
    flex: 1,
    marginBottom: 10,
  },
});

export default styles;
